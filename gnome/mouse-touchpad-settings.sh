#!/usr/bin/env bash

# Apply some mouse and touchpad Gnome settings

gsettings set org.gnome.desktop.peripherals.touchpad tap-to-click true
gsettings set org.gnome.desktop.peripherals.mouse accel-profile "flat"
